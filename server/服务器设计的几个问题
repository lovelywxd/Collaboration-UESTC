1. 编码的问题 unicode, UTF-8, GBK, ASCII。  编码的转换细节问题（中文编码）.  中文密码和用户名会怎么样？

cookie:中文的encode问题  网络传输过来的信息编码。 服务器如何处理？

使用：encoding：utf-8    （在IDE环境下可以，在linux环境必须设置默认系统编码才可以）（把代码换进去还是不行）

糊里糊涂的编码

import sys
reload(sys)
sys.setdefaultencoding("utf-8")   


2. request和response的cookie和session问题。 

3. 如何使用Django的用户登录注册问题？ 登录认证模块。

4. 下一步的拼单、下单问题。最优拼单。 拼购问题。

5. 分页的问题.  获得前20条数据

6. 跑不通crawler.py那边的代码。 爬虫数据一直存不到数据库中。找不到原因。 估计是因为数据库不匹配

7. 某些活动不存在活动列表,  添加活动类型。

8. 关于收藏的后期处理。（收藏信息）。

9. 版本合并与版本处理。  本地与远端服务器版本同步的问题。  自动化部署工具（fabric）。
   
   要将最新的合并版本推到master/server中去。YSGwebApp (目前保持的最新版本)

   备份以前的服务器。

10. 关于bookPriceList的数据仍然重复

	